# Dữ liệu production KHÔNG BAO GIỜ vào repo.
# tools/run-shadow.js nhận đường dẫn từ tham số, không có mặc định trỏ vào cây nguồn.
*-subset.json
fs-subset.json
rtdb-subset.json
*export*.json
*export*.zip
upload/
data/
